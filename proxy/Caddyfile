{
    auto_https off
    admin off
}

:{$PORT:8080} {
    handle /health {
        respond "ok" 200
    }
    reverse_proxy {$UPSTREAM} {
        transport http {
            read_timeout 60s
            dial_timeout 5s
        }
    }
}