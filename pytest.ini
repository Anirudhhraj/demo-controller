[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    integration: hits real GCP, opt-in via RUN_INTEGRATION=1
filterwarnings =
    ignore::FutureWarning:google.api_core._python_version_support